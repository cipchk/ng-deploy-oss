{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "ng-deploy-oss",
  "title": "ng-deploy-oss Schema",
  "properties": {
    "noBuild": {
      "type": "boolean",
      "default": false,
      "description": "Skip build process during deployment."
    }
  },
  "additionalProperties": true
}
