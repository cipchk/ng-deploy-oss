export const MESSAGES = {
  input_preDeletedFiles: `是否上传前预删除所有文件，否则以覆盖的形式上传`,
  input_prefix: `请输入路径前缀，如果不指定表示放在根目录下：`,
  input_buildCommand: `请输入构建生产环境的 NPM 命令行（例如：npm run build），若为空表示自动根据 angular.json 构建生成环境`,
};
